<%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <th>Airport</th>
      <th>Airline</th>
      <th>Departs</th>
      <th>More Details</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% flights.forEach(function(f) { %>
      <tr>
        <td><%= f.airport %></td>
        <td><%= f.airline %></td>
        <td><%= f.departs.toISOString().slice(0, 16) %></td>
        <td><a href="/flights/<%= f._id %>">DETAILS</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>